describe 'Arrays', ->

  it 'core', ->
    corals([]).then().should.eql []

  describe 'Given', ->
  describe 'When', ->

    it.skip 'value', ->
      corals([4, 42, 420]).when
        when: 42
        then: true
      .should.be.true()


  describe 'Then', ->
